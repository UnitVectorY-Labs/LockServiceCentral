component_management:
  individual_components:
    - component_id: "api"
      paths:
        - "api/**"
    - component_id: "locker"
      paths:
        - "locker/**"
    - component_id: "locker-firestore"
      paths:
        - "locker-firestore/**"
    - component_id: "locker-memory"
      paths:
        - "locker-memory/**"
    - component_id: "locker-etcd"
      paths:
        - "locker-etcd/**"
    - component_id: "locker-dynamodb"
      paths:
        - "locker-dynamodb/**"
    - component_id: "logging"
      paths:
        - "logging/**"
