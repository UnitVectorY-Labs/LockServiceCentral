component_management:
  individual_components:
    - component_id: "api"
      paths:
        - "api/**"
    - component_id: "locker"
      paths:
        - "locker/**"
    - component_id: "locker-firestore"
      paths:
        - "locker-firestore/**"
    - component_id: "locker-memory"
      paths:
        - "locker-memory/**"
